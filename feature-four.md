FR-004: Thử thách quiz
Mục đích: Cho phép người dùng tham gia trả lời quiz và đạt hiệu quả học tập.

Tổng quan: Người dùng đăng nhập có thể thử thách các quiz set đã công khai. Sau khi trả lời hết câu hỏi, hệ thống chấm điểm tự động và lưu lại điểm. Phân biệt điểm lần đầu và các lần sau.

4.1 Bắt đầu thử thách
Người dùng đăng nhập có thể bắt đầu thử thách quiz set đã công khai.

Ràng buộc (riêng bắt đầu):

Chỉ có thể thử thách quiz set có ít nhất 1 câu hỏi.
Khi bắt đầu, câu hỏi được hiển thị lần lượt.
Quiz set không công khai: chỉ người tạo mới được thử thách.
4.2 Nhập câu trả lời
Người dùng chọn 1 trong các lựa chọn cho mỗi câu hỏi.

Ràng buộc (riêng trả lời):

Mỗi câu hỏi phải chọn đúng 1 lựa chọn.
Phải trả lời hết tất cả câu hỏi thì thử thách mới hoàn thành.
4.3 Chấm điểm & hiển thị kết quả
Sau khi trả lời xong, hệ thống chấm điểm tự động và hiển thị kết quả.

Ràng buộc (riêng hiển thị kết quả):

Hiển thị số câu đúng và tổng số câu hỏi.
Điểm = (số câu đúng / tổng số câu hỏi × 100), làm tròn xuống phần thập phân.
Hiển thị đúng/sai cho từng câu và đáp án đúng.
4.4 Ghi nhận điểm
Sau khi hoàn thành thử thách, điểm được lưu vào hồ sơ của người dùng.

Ràng buộc (riêng ghi nhận điểm):

Người dùng có thể thử thách cùng một quiz set nhiều lần.
Lưu tất cả điểm của mỗi lần thử thách.
Điểm lần đầu được ghi là “điểm lần đầu”.
Điểm từ lần thứ hai trở đi ghi là “điểm thông thường”.
Ràng buộc (toàn bộ thử thách):

Người chưa đăng nhập không thể thử thách.
Có thể thử thách cả quiz set do chính mình tạo.
FR-005: Hiển thị điểm và bảng xếp hạng
Mục đích: Trực quan hóa kết quả học tập và thúc đẩy động lực cạnh tranh.

Tổng quan: Người dùng có thể xem lịch sử thử thách của mình. Ở mỗi quiz set, hiển thị bảng xếp hạng toàn bộ người dùng. Bảng xếp hạng chỉ phản ánh điểm lần đầu.

5.1 Hiển thị điểm của tôi (My Score)
Người dùng đăng nhập có thể xem toàn bộ lịch sử thử thách của mình.

Ràng buộc (riêng My Score):

Hiển thị tên quiz set, điểm, thời gian thử thách, phân biệt lần đầu/thông thường.
Sắp xếp theo thứ tự mới nhất.
5.2 Xếp hạng theo quiz set
Tất cả người dùng có thể xem bảng xếp hạng của mỗi quiz set.

Ràng buộc (riêng xếp hạng):

Bảng xếp hạng hiển thị ở màn chi tiết quiz set.
Tổng hợp điểm lần đầu của mỗi người dùng.
Sắp xếp điểm từ cao xuống, hiển thị Top 10.
Hiển thị tên người dùng và điểm.
Người chưa có thử thách lần đầu không xuất hiện trong bảng xếp hạng.
Ràng buộc (toàn bộ hiển thị điểm/xếp hạng):

Nếu điểm bằng nhau, người thử thách sớm hơn xếp trên.
Quiz set chưa có bản ghi thử thách thì bảng xếp hạng trống.
Quiz set không công khai: bảng xếp hạng chỉ người tạo mới xem được.
FR-006: Chức năng đánh giá bằng sao
Mục đích: Cho phép người dùng đánh giá chất lượng quiz set và hỗ trợ người khác lựa chọn.

Tổng quan: Người dùng đăng nhập có thể chấm điểm 5 mức (1–5 sao) cho quiz set đã công khai. Hiển thị điểm đánh giá trung bình của mỗi quiz set.

6.1 Gửi đánh giá
Người dùng đăng nhập có thể đánh giá quiz set đã công khai.

Ràng buộc (riêng gửi đánh giá):

Mỗi người dùng chỉ đánh giá 1 lần cho 1 quiz set.
Điểm đánh giá là số nguyên 1–5.
Có thể đánh giá cả quiz set do chính mình tạo.
6.2 Thay đổi đánh giá
Người dùng đăng nhập có thể thay đổi đánh giá đã gửi.

Ràng buộc (riêng thay đổi):

Chỉ thay đổi đối với quiz set đã được đánh giá trước đó.
Điểm đánh giá sau thay đổi vẫn là số nguyên 1–5.
6.3 Xóa đánh giá
Người dùng đăng nhập có thể xóa đánh giá của chính mình.

6.4 Hiển thị điểm trung bình
Tất cả người dùng có thể xem điểm đánh giá trung bình của từng quiz set.

Ràng buộc (riêng hiển thị trung bình):

Hiển thị đến 1 chữ số thập phân.
Nếu chưa có đánh giá nào, hiển thị “Không có đánh giá”.
Hiển thị ở cả danh sách và chi tiết quiz set.
Ràng buộc (toàn bộ chức năng đánh giá):

Người chưa đăng nhập không thể gửi/sửa/xóa đánh giá.
Quiz set không công khai: chỉ người tạo mới có thể đánh giá.
Khi quiz set bị xóa, toàn bộ đánh giá liên quan cũng bị xóa.