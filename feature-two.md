FR-002: Quản lý quiz set
Mục đích: Cho phép người dùng tạo/quản lý quiz set để cung cấp nội dung học tập.

Tổng quan: Người dùng đăng nhập có thể tạo quiz set gồm nhiều câu hỏi. Quiz set có tiêu đề, mô tả, danh mục, trạng thái công khai và do người tạo quản lý.

2.1 Tạo quiz set
Người dùng đăng nhập có thể tạo quiz set mới.

Ràng buộc (riêng tạo):

Bắt buộc: tiêu đề.
Mô tả và danh mục là tùy chọn.
Lúc tạo có thể chưa có câu hỏi.
Trạng thái mặc định lúc tạo là không công khai.
2.2 Danh sách quiz set
Tất cả người dùng (kể cả chưa đăng nhập) có thể xem danh sách quiz set đã công khai.

Ràng buộc (riêng danh sách):

Hiển thị: tiêu đề, tên người tạo, danh mục, số câu hỏi, điểm đánh giá trung bình, số người tham gia.
Số người tham gia: số người dùng duy nhất đã từng thử thách quiz set đó.
Có thể lọc theo danh mục.
Quiz set không công khai không hiển thị trong danh sách.
Người tạo có thể thấy cả quiz set không công khai của mình trong danh sách.
2.3 Chi tiết quiz set
Tất cả người dùng có thể xem chi tiết quiz set đã công khai.

Ràng buộc (riêng chi tiết):

Hiển thị: tiêu đề, mô tả, tên người tạo, danh mục, số câu hỏi, đánh giá trung bình, số người tham gia, bảng xếp hạng.
Số người tham gia là số người dùng duy nhất đã thử thách quiz set đó.
Không hiển thị nội dung câu hỏi (chỉ hiển thị sau khi bắt đầu thử thách).
Quiz set không công khai chỉ người tạo mới xem được.
2.4 Chỉnh sửa quiz set
Chỉ người tạo mới chỉnh sửa quiz set của chính mình.

Ràng buộc (riêng chỉnh sửa):

Có thể thay đổi tiêu đề, mô tả, danh mục.
Sửa câu hỏi thực hiện ở chức năng quản lý câu hỏi.
2.5 Xóa quiz set
Chỉ người tạo mới xóa quiz set của chính mình.

Ràng buộc (riêng xóa):

Xóa quiz set sẽ xóa toàn bộ câu hỏi liên quan.
Lịch sử thử thách trong quá khứ vẫn được giữ.
Các đánh giá gắn với quiz set bị xóa cũng sẽ bị xóa.
2.6 Cài đặt công khai/không công khai
Người tạo có thể chuyển đổi trạng thái công khai của quiz set.

Ràng buộc (riêng trạng thái công khai):

Chỉ có “công khai” hoặc “không công khai”.
Quiz set không công khai: người khác không truy cập được.
Quiz set không công khai: không xuất hiện trong danh sách (nhưng hiển thị với người tạo).
Quiz set không công khai: người khác không thể tham gia (trừ người tạo).
Ràng buộc (toàn bộ quản lý quiz set):

Người không phải người tạo không thể sửa/xóa/đổi trạng thái công khai.
Người chưa đăng nhập không thể tạo/sửa/xóa quiz set.